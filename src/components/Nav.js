import React from 'react';
import AppBar from 'material-ui/AppBar';

const Nav = (props) => {
  return (
    <div>
      <AppBar
        title="Adopt-a-Family"
      />
    </div>
  )
};

export default Nav;